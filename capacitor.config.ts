import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'ionic.quick.start',
  appName: 'ionic-quick-start',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
